#!/usr/bin/env python3
"""A program that takes two strings and return a concatenated string
of both strings"""


def concat(str1: str, str2: str) -> str:
    """The function to concatenate the two strings"""
    return (str1 + str2)
